/**
 * General Utility Hooks Index
 * Client-side utility hooks that don't depend on React Query
 *
 * Usage:
 * import { useDebounce, useMediaQuery, useRequireAuth } from "@/hooks";
 */

export { useDebounce } from "./useDebounce";
export { useRequireAuth } from "./useRequireAuth";
export { useToast } from "./useToast";
export { useMediaQuery } from "./useMediaQuery";
